global:
  scrape_interval: 5s
  evaluation_interval: 30s

scrape_configs:
  - job_name: bmc
    metrics_path: /metrics
    static_configs:
      - targets:
          - localhost:41611
          #- localhost:5003
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
